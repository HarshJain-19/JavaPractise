package problems.streams;

import problems.project2.model.Employee;

import java.util.stream.Collectors;

public class Practise2 {
    public static void main(String[] args) {
//        Ques.1 Find list of employees who live in Same City and their average salaries are more than 1 lakh
//
//        employees.stream()
//                .collect(Collectors.groupingBy((Employee emp) -> emp.getAddress().getCity()))
//                .entrySet().stream()
//                .filter(entry -> entry.getValue().stream()
//                        .collect(Collectors.averagingDouble(Employee::getBaseSalary)) > 30000)
//                .forEach(System.out::println);
    }
}
